##  -- GENERATED FILE
[ -d "$packdir/plugins-clj/start/cljfmt" ] || { mkdir -p "$packdir/plugins-clj/start/cljfmt" && cd "$packdir/plugins-clj/start" && git clone --recursive https://github.com/weavejester/cljfmt.git cljfmt; } ## plugins-clj/start/cljfmt
[ -d "$packdir/plugins-clj/start/eastwood" ] || { mkdir -p "$packdir/plugins-clj/start/eastwood" && cd "$packdir/plugins-clj/start" && git clone --recursive https://github.com/venantius/vim-eastwood.git eastwood; } ## plugins-clj/start/eastwood
[ -d "$packdir/plugins-clj/start/fireplace" ] || { mkdir -p "$packdir/plugins-clj/start/fireplace" && cd "$packdir/plugins-clj/start" && git clone --recursive https://github.com/tpope/vim-fireplace.git fireplace; } ## plugins-clj/start/fireplace
[ -d "$packdir/plugins-color/start/ayu" ] || { mkdir -p "$packdir/plugins-color/start/ayu" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/ayu-theme/ayu-vim.git ayu; } ## plugins-color/start/ayu
[ -d "$packdir/plugins-color/start/deus" ] || { mkdir -p "$packdir/plugins-color/start/deus" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/ajmwagar/vim-deus.git deus; } ## plugins-color/start/deus
[ -d "$packdir/plugins-color/start/gotham" ] || { mkdir -p "$packdir/plugins-color/start/gotham" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/whatyouhide/vim-gotham.git gotham; } ## plugins-color/start/gotham
[ -d "$packdir/plugins-color/start/gruvbox" ] || { mkdir -p "$packdir/plugins-color/start/gruvbox" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/morhetz/gruvbox.git gruvbox; } ## plugins-color/start/gruvbox
[ -d "$packdir/plugins-color/start/hybrid" ] || { mkdir -p "$packdir/plugins-color/start/hybrid" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/w0ng/vim-hybrid.git hybrid; } ## plugins-color/start/hybrid
[ -d "$packdir/plugins-color/start/hybrid-material" ] || { mkdir -p "$packdir/plugins-color/start/hybrid-material" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/kristijanhusak/vim-hybrid-material.git hybrid-material; } ## plugins-color/start/hybrid-material
[ -d "$packdir/plugins-color/start/iceberg" ] || { mkdir -p "$packdir/plugins-color/start/iceberg" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/cocopon/iceberg.vim.git iceberg; } ## plugins-color/start/iceberg
[ -d "$packdir/plugins-color/start/lucid" ] || { mkdir -p "$packdir/plugins-color/start/lucid" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/cseelus/vim-colors-lucid.git colors-lucid; } ## plugins-color/start/lucid
[ -d "$packdir/plugins-color/start/molokai" ] || { mkdir -p "$packdir/plugins-color/start/molokai" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/tomasr/molokai.git molokai; } ## plugins-color/start/molokai
[ -d "$packdir/plugins-color/start/monochrome" ] || { mkdir -p "$packdir/plugins-color/start/monochrome" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/fxn/vim-monochrome.git monochrome; } ## plugins-color/start/monochrome
[ -d "$packdir/plugins-color/start/nord" ] || { mkdir -p "$packdir/plugins-color/start/nord" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/arcticicestudio/nord-vim.git nord; } ## plugins-color/start/nord
[ -d "$packdir/plugins-color/start/oceanic-next" ] || { mkdir -p "$packdir/plugins-color/start/oceanic-next" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/mhartington/oceanic-next.git oceanic-next; } ## plugins-color/start/oceanic-next
[ -d "$packdir/plugins-color/start/one" ] || { mkdir -p "$packdir/plugins-color/start/one" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/rakr/vim-one.git one; } ## plugins-color/start/one
[ -d "$packdir/plugins-color/start/onedark" ] || { mkdir -p "$packdir/plugins-color/start/onedark" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/joshdick/onedark.vim.git onedark; } ## plugins-color/start/onedark
[ -d "$packdir/plugins-color/start/papercolor-theme" ] || { mkdir -p "$packdir/plugins-color/start/papercolor-theme" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/NLKNguyen/papercolor-theme.git papercolor-theme; } ## plugins-color/start/papercolor-theme
[ -d "$packdir/plugins-color/start/pink-moon" ] || { mkdir -p "$packdir/plugins-color/start/pink-moon" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/sts10/vim-pink-moon.git pink-moon; } ## plugins-color/start/pink-moon
[ -d "$packdir/plugins-color/start/pyte" ] || { mkdir -p "$packdir/plugins-color/start/pyte" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/vim-scripts/pyte.git pyte; } ## plugins-color/start/pyte
[ -d "$packdir/plugins-color/start/seoul256" ] || { mkdir -p "$packdir/plugins-color/start/seoul256" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/junegunn/seoul256.vim.git seoul256; } ## plugins-color/start/seoul256
[ -d "$packdir/plugins-color/start/snow" ] || { mkdir -p "$packdir/plugins-color/start/snow" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/nightsense/snow.git snow; } ## plugins-color/start/snow
[ -d "$packdir/plugins-color/start/solarized" ] || { mkdir -p "$packdir/plugins-color/start/solarized" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/altercation/solarized solarized; } ## plugins-color/start/solarized
[ -d "$packdir/plugins-color/start/solarized8" ] || { mkdir -p "$packdir/plugins-color/start/solarized8" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/lifepillar/vim-solarized8.git solarized8; } ## plugins-color/start/solarized8
[ -d "$packdir/plugins-color/start/summerfruit256" ] || { mkdir -p "$packdir/plugins-color/start/summerfruit256" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/mightwork/summerfruit256.vim.git summerfruit256; } ## plugins-color/start/summerfruit256
[ -d "$packdir/plugins-color/start/tomorrow-theme" ] || { mkdir -p "$packdir/plugins-color/start/tomorrow-theme" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/chriskempson/tomorrow-theme.git tomorrow-theme; } ## plugins-color/start/tomorrow-theme
[ -d "$packdir/plugins-color/start/xterm-color-table" ] || { mkdir -p "$packdir/plugins-color/start/xterm-color-table" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/guns/xterm-color-table.vim.git xterm-color-table; } ## plugins-color/start/xterm-color-table
[ -d "$packdir/plugins-syn/start/ale" ] || { mkdir -p "$packdir/plugins-syn/start/ale" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/w0rp/ale.git ale; } ## plugins-syn/start/ale
[ -d "$packdir/plugins-syn/start/autotag" ] || { mkdir -p "$packdir/plugins-syn/start/autotag" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/craigemery/vim-autotag.git autotag; } ## plugins-syn/start/autotag
[ -d "$packdir/plugins-syn/start/closer" ] || { mkdir -p "$packdir/plugins-syn/start/closer" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/rstacruz/vim-closer.git closer; } ## plugins-syn/start/closer
[ -d "$packdir/plugins-syn/start/commentary" ] || { mkdir -p "$packdir/plugins-syn/start/commentary" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/tpope/vim-commentary.git commentary; } ## plugins-syn/start/commentary
[ -d "$packdir/plugins-syn/start/endwise" ] || { mkdir -p "$packdir/plugins-syn/start/endwise" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/tpope/vim-endwise.git endwise; } ## plugins-syn/start/endwise
[ -d "$packdir/plugins-syn/start/fastfold" ] || { mkdir -p "$packdir/plugins-syn/start/fastfold" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/Konfekt/FastFold.git fastfold; } ## plugins-syn/start/fastfold
[ -d "$packdir/plugins-syn/start/indent-sh" ] || { mkdir -p "$packdir/plugins-syn/start/indent-sh" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/Clavelito/indent-sh.vim.git indent-sh; } ## plugins-syn/start/indent-sh
[ -d "$packdir/plugins-syn/start/polyglot" ] || { mkdir -p "$packdir/plugins-syn/start/polyglot" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/sheerun/vim-polyglot.git polyglot; } ## plugins-syn/start/polyglot
[ -d "$packdir/plugins-syn/start/prettier" ] || { mkdir -p "$packdir/plugins-syn/start/prettier" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/prettier/vim-prettier.git prettier; } ## plugins-syn/start/prettier
[ -d "$packdir/plugins-syn/start/repl" ] || { mkdir -p "$packdir/plugins-syn/start/repl" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/sillybun/vim-repl.git repl; } ## plugins-syn/start/repl
[ -d "$packdir/plugins-syn/start/simpylfold" ] || { mkdir -p "$packdir/plugins-syn/start/simpylfold" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/tmhedberg/SimpylFold.git simpylfold; } ## plugins-syn/start/simpylfold
[ -d "$packdir/plugins-syn/start/snippets" ] || { mkdir -p "$packdir/plugins-syn/start/snippets" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/honza/vim-snippets.git snippets; } ## plugins-syn/start/snippets
[ -d "$packdir/plugins-syn/start/ultisnips" ] || { mkdir -p "$packdir/plugins-syn/start/ultisnips" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/SirVer/ultisnips.git ultisnips; } ## plugins-syn/start/ultisnips
[ -d "$packdir/plugins-syn/start/youcompleteme" ] || { mkdir -p "$packdir/plugins-syn/start/youcompleteme" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/ycm-core/YouCompleteMe.git youcompleteme; } ## plugins-syn/start/youcompleteme
[ -d "$packdir/plugins-ui/start/boxdraw" ] || { mkdir -p "$packdir/plugins-ui/start/boxdraw" && cd "$packdir/plugins-ui/start" && git clone --recursive https://github.com/gyim/vim-boxdraw.git boxdraw; } ## plugins-ui/start/boxdraw
[ -d "$packdir/plugins-ui/start/lightline" ] || { mkdir -p "$packdir/plugins-ui/start/lightline" && cd "$packdir/plugins-ui/start" && git clone --recursive https://github.com/itchyny/lightline.vim.git lightline; } ## plugins-ui/start/lightline
[ -d "$packdir/plugins-ui/start/vimux" ] || { mkdir -p "$packdir/plugins-ui/start/vimux" && cd "$packdir/plugins-ui/start" && git clone --recursive https://github.com/benmills/vimux.git vimux; } ## plugins-ui/start/vimux
[ -d "$packdir/plugins-util/start/abolish" ] || { mkdir -p "$packdir/plugins-util/start/abolish" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-abolish.git abolish; } ## plugins-util/start/abolish
[ -d "$packdir/plugins-util/start/ack" ] || { mkdir -p "$packdir/plugins-util/start/ack" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/mileszs/ack.vim.git ack; } ## plugins-util/start/ack
[ -d "$packdir/plugins-util/start/clam" ] || { mkdir -p "$packdir/plugins-util/start/clam" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/sjl/clam.vim.git clam; } ## plugins-util/start/clam
[ -d "$packdir/plugins-util/start/dirdiff" ] || { mkdir -p "$packdir/plugins-util/start/dirdiff" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/will133/vim-dirdiff.git dirdiff; } ## plugins-util/start/dirdiff
[ -d "$packdir/plugins-util/start/fugitive" ] || { mkdir -p "$packdir/plugins-util/start/fugitive" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-fugitive.git fugitive; } ## plugins-util/start/fugitive
[ -d "$packdir/plugins-util/start/fzf" ] || { mkdir -p "$packdir/plugins-util/start/fzf" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/junegunn/fzf.vim.git fzf; } ## plugins-util/start/fzf
[ -d "$packdir/plugins-util/start/kwbd" ] || { mkdir -p "$packdir/plugins-util/start/kwbd" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/rgarver/Kwbd.vim.git kwbd; } ## plugins-util/start/kwbd
[ -d "$packdir/plugins-util/start/matchit" ] || { mkdir -p "$packdir/plugins-util/start/matchit" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tmhedberg/matchit.git matchit; } ## plugins-util/start/matchit
[ -d "$packdir/plugins-util/start/surround" ] || { mkdir -p "$packdir/plugins-util/start/surround" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-surround.git surround; } ## plugins-util/start/surround
[ -d "$packdir/plugins-util/start/vinegar" ] || { mkdir -p "$packdir/plugins-util/start/vinegar" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-vinegar.git vinegar; } ## plugins-util/start/vinegar
